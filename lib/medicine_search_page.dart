// medicine_search_page.dart

import 'package:flutter/material.dart';

class MedicineSearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Medicine Search'),
      ),
      body: Center(
        child: Text('Medicine Search Page'),
      ),
    );
  }
}
